/// Enum representing different time periods for data aggregation
enum TimePeriod {
  /// Single day view
  day,
  
  /// Week view (7 days)
  week,
  
  /// Month view (30 days)
  month,
  
  /// Year view (12 months)
  year
}